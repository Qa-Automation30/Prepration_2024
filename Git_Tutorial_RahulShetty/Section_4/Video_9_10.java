package Git_Tutorial_RahulShetty.Section_4;

// No need to watch it again. Notes are enough

public class Video_9_10 {
    /**
     * So now how do you know on which branch you are ->
     * "git branch"  is the command -> which tells you on which branch you are currently
     */
    //=====================================================
    /**
     * Now it is time to cut the branch from the master -->
     * command is --> [ git checkout -b "new_feature_branch" ]
     * So this command creates a new branch and at the same time it will switch to the new branch as well.
     */
    //====================================================
    /**
     * How to switch to the branches  -->
     * git checkout "put the branch name here"
     */
}
